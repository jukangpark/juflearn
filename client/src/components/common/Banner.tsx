import styled from "styled-components";

const StyledBanner = styled.div`
  height: 320px;
  background-color: whitesmoke;
`;

const Banner = () => {
  return <StyledBanner>배너 영역</StyledBanner>;
};

export default Banner;
