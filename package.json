{
  "name": "juflearn",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client-build": "cd client && npm ci && npm install webpack-cli && npm run build && cd ../",
    "server-build": "cd server && npm ci --dev && npm run build && cd ../",
    "heroku-postbuild": "webpack-cli",
    "heroku-prebuild": "npm run client-build && npm run server-build && mv ./client/dist ./server/dist/",
    "start": "cd server && npm ci && tsc && node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jukangpark/juflearn.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jukangpark/juflearn/issues"
  },
  "homepage": "https://github.com/jukangpark/juflearn#readme"
}
